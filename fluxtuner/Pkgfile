# Description: Modern internet radio player for terminal, desktop and web
# URL: https://github.com/pitill0/fluxtuner
# Maintainer: Victor Martinez, pitillo at crux-arm dot nu
# Depends on: mpv python3-requests python3-textual python3-mdit-py-plugins

name=fluxtuner
version=1.1.0
release=1
source=(https://github.com/pitill0/${name}/archive/refs/tags/v${version}.tar.gz)

build() {
  cd ${name}-${version}

  /usr/bin/python3 -m build --wheel --skip-dependency-check --no-isolation
  /usr/bin/python3 -m installer --compile-bytecode 2 --destdir=${PKG} dist/*.whl

  # Web support is provided by the separate fluxtuner-web port.
  rm -f ${PKG}/usr/bin/fluxtuner-web
}
